"use client";

/**
 * Auth-scoped motion wrappers — built on shared flow-motion primitives.
 * Use flow-motion directly for booking, onboarding, and other multi-step flows.
 */
export {
  FlowBoxReveal as AuthBoxReveal,
  FlowRipple as AuthRipple,
  FlowOrbitRing as AuthOrbitRing,
  FlowGlowInput as AuthGlowInput,
} from "@/components/shared/flow-motion";
